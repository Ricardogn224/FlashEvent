Skip to content
Navigation Menu
Ricardogn224
/
FlashEvent

Type / to search
Code
Issues
Pull requests
Actions
Projects
Wiki
Security
2
Insights
Settings
Go CI
fix conflit and bottom nav #47
Jobs
Run details
Workflow file for this run
.github/workflows/ci.yml at 360bdb5
name: Go CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10  # Augmentez la limite de temps à 10 minutes

    strategy:
      matrix:
        go-version: ['1.20', '1.21.x']

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}

      - name: Display Go version
        run: go version

      - name: Install dependencies
        run: |
          echo "Running go mod tidy"
          go mod tidy
        working-directory: ./backend

      - name: Run tests
        run: |
          echo "Running tests"
          go test -v ./test/...
        working-directory: ./backend

    
